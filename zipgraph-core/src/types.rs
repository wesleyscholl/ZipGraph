//! Type aliases and common types used throughout ZipGraph Core

/// Node identifier type
pub type NodeId = usize;

/// Edge weight type
pub type Weight = f64;

/// Feature vector type for node properties
pub type FeatureVector = Vec<f64>;
